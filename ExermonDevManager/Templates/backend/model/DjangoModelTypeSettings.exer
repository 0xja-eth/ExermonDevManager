$[language=python]\
# region 转化读取配置

${keyName?KEY_NAME = ${keyName}
}\
${hasTypeSettings?\
TYPE_FIELD_FILTER_MAP = \{
	$<typeSettings:'${name}': [${genFieldsCode}]
>$(,)\
\}

TYPE_RELATED_FILTER_MAP = \{
	$<typeSettings:'${name}': [${genRelsCode}]
>$(,)\
\}
}
# endregion